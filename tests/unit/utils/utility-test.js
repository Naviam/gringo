import utility from '../../../utils/utility';
import { module, test } from 'qunit';

module('Unit | Utility | utility');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = utility();
  assert.ok(result);
});
